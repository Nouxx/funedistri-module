# -*- coding: utf-8 -*-
# A module's top-level __init__.py runs when Odoo imports the module. It wires up
# the Python sub-packages (models, controllers, …). Empty for now — there is no
# Python code yet (Step 0 is scaffold only). As we add code we will import the
# sub-packages here, e.g.:
#     from . import models
#     from . import controllers
