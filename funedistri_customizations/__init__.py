# -*- coding: utf-8 -*-
# A module's top-level __init__.py runs when Odoo imports the module. It wires up
# the Python sub-packages (models, controllers, …).
#
# FRESH START (2026-06-15): empty — there is no Python yet. As steps land, add
# imports here (e.g. `from . import models`), models before controllers so any
# fields exist before a route references them.
